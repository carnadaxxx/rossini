import React from 'react';
import Typography from '@material-ui/core/Typography';

const NotFound = () => {
    return (
        <div>
            <Typography variant="h1" component="h2" gutterBottom>
                404 not found psss mascota >=/
            </Typography>
        </div>
    );
};

export default NotFound;
